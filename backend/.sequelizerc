const {resolve} = require('path')

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'pgConfig.js'),
    'models-path': resolve(__dirname, 'src', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'database', 'pg', 'migrations'),
    'seeders-path': resolve(__dirname, 'src', 'database', 'pg', 'seeds')
}
